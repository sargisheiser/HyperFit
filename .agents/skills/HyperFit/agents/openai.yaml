interface:
  display_name: "HyperFit"
  short_description: "Repo-specific patterns and workflows for HyperFit"
  default_prompt: "Use the HyperFit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true