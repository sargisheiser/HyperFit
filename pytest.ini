[pytest]
addopts = --cov=backend --cov-report=term-missing -p no:asyncio
testpaths = backend/tests
markers =
    unit: pure unit tests with no DB, network, or external deps
    integration: tests hitting FastAPI TestClient and/or the test database
filterwarnings =
    ignore::DeprecationWarning









